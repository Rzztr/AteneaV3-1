const app=document.getElementById("app");function obtenerUbicacion(){return new Promise(resolve=>{if(!navigator.geolocation){console.warn("[Geolocalización] No soportada en este dispositivo");resolve(null);return}navigator.geolocation.getCurrentPosition(pos=>{console.log("[Geolocalización] ✓ GPS obtenido:",pos.coords.latitude,pos.coords.longitude);resolve({lat:pos.coords.latitude,lng:pos.coords.longitude})},()=>{console.warn("[Geolocalización] GPS falló, intentando con red/WiFi...");navigator.geolocation.getCurrentPosition(pos=>{console.log("[Geolocalización] ✓ Ubicación por red obtenida:",pos.coords.latitude,pos.coords.longitude);resolve({lat:pos.coords.latitude,lng:pos.coords.longitude})},e=>{if(e.code===1){console.warn("[Geolocalización] Permiso denegado por el usuario")}else{console.warn("[Geolocalización] No se pudo obtener ubicación tras dos intentos:",e.message)}resolve(null)},{enableHighAccuracy:false,timeout:1e4,maximumAge:3e4})},{enableHighAccuracy:true,timeout:12e3,maximumAge:0})})}window.onload=()=>{document.getElementById("btn-anon-panic").addEventListener("click",async()=>{if(confirm("¿Estás seguro de enviar una alerta de PÁNICO?")){const btn=document.getElementById("btn-anon-panic");btn.disabled=true;btn.textContent="Obteniendo ubicación...";btn.style.opacity="0.7";try{const ubicacion=await obtenerUbicacion();btn.textContent="Enviando alerta...";const res=await window.saveAnonymousIncident({lat:ubicacion?.lat??null,lng:ubicacion?.lng??null});if(res){alert(ubicacion?"¡Alerta de PÁNICO enviada con ubicación!":"¡Alerta enviada! No se pudo obtener ubicación GPS.")}else{alert("Hubo un error al enviar la alerta o faltan permisos en el servidor.")}}catch(err){console.error("[Pánico] Error inesperado:",err);alert("Hubo un error inesperado al enviar la alerta.")}finally{btn.disabled=false;btn.textContent="PÁNICO";btn.style.opacity="1"}}})};